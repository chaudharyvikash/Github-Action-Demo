import os

if os.getenv("USER_NAME"):
    values= os.getenv("USER_NAME")
    if values== "vikash@123"
        print("value match)
              else
                  print("value not matched")
    

